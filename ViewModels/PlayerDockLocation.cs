namespace SLSKDONET.ViewModels;

/// <summary>
/// Defines where the music player is docked in the UI.
/// </summary>
public enum PlayerDockLocation
{
    /// <summary>
    /// Player is docked at the bottom as a horizontal mini-player bar.
    /// </summary>
    BottomBar,
    
    /// <summary>
    /// Player is docked on the right side as a vertical full-height sidebar.
    /// </summary>
    RightSidebar
}
