// Views/SettingsPage.axaml.cs (Converted Code-Behind)

using Avalonia.Controls;
using Avalonia.Markup.Xaml;

namespace SLSKDONET.Views
{
    // Avalonia uses UserControl as the base for pages/views
    public partial class SettingsPage : UserControl
    {
        public SettingsPage()
        {
            InitializeComponent();
        }
        
        // Avalonia initializes components via the XAML loader
        private void InitializeComponent()
        {
            AvaloniaXamlLoader.Load(this);
        }
    }
}
